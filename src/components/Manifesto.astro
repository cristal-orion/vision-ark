---
/**
 * Fascia piena in grafite. Il divisore fra le sezioni è un blocco di colore
 * a tutta pagina, non un filetto: è il vocabolario del poster.
 * Diptych testo/prosa — la seconda variante della coppia Split Studio.
 */
---

<section class="manifesto on-ink" id="studio">
  <div class="shell manifesto__inner">
    <h2 class="display display--s manifesto__claim">
      <span class="display__lead">Il nostro studio</span>
      segue il cantiere.
    </h2>

    <div class="prose manifesto__prose">
      <p>
        Con <strong>Vision Ark</strong> puoi affidare la ristrutturazione
        allo stesso team che cura il progetto. I nostri progettisti studiano
        la distribuzione degli ambienti e preparano i disegni tecnici;
        in cantiere coordiniamo le squadre e i fornitori.
      </p>
      <p>
        Prima di iniziare i lavori, esaminiamo con te le planimetrie e i render.
        Puoi valutare la disposizione degli arredi, i materiali e
        l&rsquo;illuminazione per scegliere cosa realizzare.
      </p>
    </div>
  </div>
</section>

<style>
  .manifesto {
    padding-block: clamp(var(--space-2xl), 8vw, var(--space-3xl));
  }

  .manifesto__inner {
    display: grid;
    /* colonne disuguali: 1.35 a 1, il claim domina la prosa */
    grid-template-columns: minmax(0, 1.35fr) minmax(0, 1fr);
    gap: clamp(var(--space-xl), 5vw, var(--space-3xl));
    align-items: start;
  }

  .manifesto__claim {
    max-width: 24ch;
  }

  .manifesto__prose {
    /* la prosa scende di un gradino rispetto al claim: rompe l'allineamento
       alto e dà un asse di lettura in diagonale */
    padding-block-start: 0.6em;
    border-block-start: var(--rule-slab) solid var(--color-accent);
  }

  @media (width < 60rem) {
    .manifesto__inner {
      grid-template-columns: minmax(0, 1fr);
      gap: var(--space-xl);
    }

    .manifesto__claim {
      max-width: 20ch;
    }
  }
</style>
