<!-- Nessun tag esterno è presente nel markup: li carica solo lo script, dopo la scelta. -->
<section class="cookie" aria-labelledby="cookie-title" data-cookie-panel hidden>
  <div class="cookie__intro">
    <div>
      <p class="cookie__label">Vision Ark / Privacy</p>
      <h2 id="cookie-title">Scegli quali cookie usare.</h2>
    </div>
    <p class="cookie__note">
      Quelli necessari al funzionamento del sito restano attivi. Statistiche e
      marketing si avviano solo se li autorizzi. Puoi cambiare idea quando vuoi.{" "}
      <a href="/privacy#cookie">Leggi l’informativa</a>.
    </p>
  </div>

  <div class="cookie__choices">
    <div class="cookie__choice">
      <span class="cookie__choice-title">Necessari</span>
      <span class="cookie__badge">Sempre attivi</span>
      <p>Memorizzano la tua scelta sui cookie. Nessun tracciamento.</p>
    </div>
    <label class="cookie__choice cookie__choice--optional">
      <span class="cookie__choice-title">Statistiche</span>
      <input type="checkbox" data-cookie-analytics />
      <span class="cookie__check" aria-hidden="true"></span>
      <span class="cookie__description">Google Analytics 4: visite e pagine consultate.</span>
    </label>
    <label class="cookie__choice cookie__choice--optional">
      <span class="cookie__choice-title">Marketing</span>
      <input type="checkbox" data-cookie-marketing />
      <span class="cookie__check" aria-hidden="true"></span>
      <span class="cookie__description">Meta Pixel: misurazione delle campagne.</span>
    </label>
  </div>

  <div class="cookie__actions">
    <button type="button" class="cookie__button" data-cookie-reject>Rifiuta tutti</button>
    <button type="button" class="cookie__button" data-cookie-save>Salva la scelta</button>
    <button type="button" class="cookie__button cookie__button--filled" data-cookie-accept>Accetta tutti</button>
  </div>
</section>

<script>
  import "../scripts/cookie-consent";
</script>

<style>
  .cookie {
    position: fixed;
    z-index: var(--z-modal);
    inset-block-end: var(--space-md);
    inset-inline: var(--gutter);
    width: min(calc(100% - 2 * var(--gutter)), 65rem);
    max-height: calc(100dvh - 2 * var(--space-md));
    margin-inline: auto;
    padding: clamp(var(--space-md), 3vw, var(--space-xl));
    overflow-y: auto;
    border: var(--rule-slab) solid var(--color-ink);
    background: var(--color-paper);
    box-shadow: 0 0 var(--space-lg) var(--color-scrim);
  }

  .cookie__intro {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.2fr);
    align-items: end;
    gap: var(--space-sm) var(--space-lg);
  }

  .cookie__label {
    margin-block-end: var(--space-xs);
    font-family: var(--font-outlier);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-label);
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .cookie h2 {
    font-family: var(--font-display);
    font-size: clamp(var(--text-md), 2.2vw + 0.65rem, var(--text-xl));
    font-weight: 800;
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-display);
    text-transform: uppercase;
  }

  .cookie__note,
  .cookie__choice p,
  .cookie__description {
    font-size: var(--text-sm);
    line-height: 1.45;
    color: var(--color-ink-2);
  }

  .cookie__note a {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .cookie__choices {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-md);
    margin-block: var(--space-lg);
  }

  .cookie__choice {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-content: start;
    gap: var(--space-2xs) var(--space-xs);
    padding-block-start: var(--space-sm);
    border-block-start: var(--rule-slab) solid var(--color-ink);
  }

  .cookie__choice-title {
    font-family: var(--font-display);
    font-size: var(--text-md);
    font-weight: 700;
    line-height: var(--leading-tight);
    text-transform: uppercase;
  }

  .cookie__badge {
    align-self: center;
    font-family: var(--font-outlier);
    font-size: var(--text-xs);
    color: var(--color-muted);
    white-space: nowrap;
  }

  .cookie__choice p,
  .cookie__description {
    grid-column: 1 / -1;
  }

  .cookie__choice--optional {
    min-height: 44px;
    cursor: pointer;
  }

  .cookie__choice--optional input {
    position: absolute;
    inset-block-start: var(--space-sm);
    inset-inline-end: 0;
    width: 1.5rem;
    height: 1.5rem;
    opacity: 0;
    cursor: pointer;
  }

  .cookie__check {
    width: 1.5rem;
    height: 1.5rem;
    border: var(--rule-slab) solid var(--color-ink);
    background: var(--color-paper);
  }

  .cookie__choice--optional input:checked + .cookie__check {
    background: var(--color-ink);
    box-shadow: inset 0 0 0 0.3rem var(--color-paper);
  }

  .cookie__choice--optional input:focus-visible + .cookie__check {
    outline: var(--rule-slab) solid var(--color-focus);
    outline-offset: 2px;
  }

  .cookie__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-sm);
    padding-block-start: var(--space-md);
    border-block-start: var(--rule-hair) solid var(--color-rule);
  }

  .cookie__button {
    min-height: 44px;
    padding: var(--space-sm) var(--space-md);
    border: var(--rule-slab) solid var(--color-ink);
    background: transparent;
    font-family: var(--font-outlier);
    font-size: var(--text-sm);
    font-weight: 500;
    cursor: pointer;
  }

  .cookie__button:hover {
    background: var(--color-paper-2);
  }

  .cookie__button--filled {
    background: var(--color-ink);
    color: var(--color-on-ink);
  }

  .cookie__button--filled:hover {
    background: var(--color-ink-2);
  }

  @media (width < 48rem) {
    .cookie__intro {
      grid-template-columns: minmax(0, 1fr);
    }

    .cookie__choices {
      grid-template-columns: minmax(0, 1fr);
      gap: var(--space-sm);
      margin-block: var(--space-md);
    }

    .cookie__actions {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .cookie__button[data-cookie-save] {
      grid-column: 1 / -1;
      grid-row: 2;
    }
  }

  @media (width < 30rem) {
    .cookie__button {
      padding-inline: var(--space-xs);
      font-size: var(--text-xs);
    }
  }
</style>
